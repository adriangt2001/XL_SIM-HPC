# Models configs
main_model_name: Swin2SR
main_model_config: configs/models/swin2srX2.json
checkpoint: runs/Swin2SR/run_20260629_093149/best_psnr

comparison_model_names: [Basic, Basic, Basic, RL, RL, RL]
comparison_model_configs: [configs/models/rlX2_mean.json, configs/models/rlX2_max.json, configs/models/rlX2_sum.json, configs/models/rlX2_mean.json, configs/models/rlX2_max.json, configs/models/rlX2_sum.json]
comparison_checkpoints: ["", "", "", "", "", ""]

upscale: 2

# Dataset, preprocessing and postprocessing
dataset: data/LSDIR
split: validation
test_size: 0.99
batch_size: 8
num_workers: 8
first_crop: 1024
second_crop: 512

# Simulator
microscope_config: configs/simulator/default_microscope.yaml
noise_config: configs/simulator/no_noise.yaml